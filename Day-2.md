## _How to install React:_

- check Nodejs version as well whether it is installed or not

  - version check:
    - node -v
    - node --version

- npx creact-react-app (fileName)
- cd (fileName) => require only when you are in different directory
- npm start

---
